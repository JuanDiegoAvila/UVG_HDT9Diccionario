import java.util.Map;

public class Factory {
    public Map factory(int opcion){
        switch (opcion){
            case 1 -> { //regresa un splay tree;
            }
            case 2 -> { //regresa un arbol 2-3;
            }
        }
        return null;
    }
}
